{
  "name": "codes",
  "version": "1.0.0",
  "description": "A collaborative virtual study space where students can join study rooms, use Pomodoro timers, chat with peers, and enter focus mode for distraction-free studying.",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "dev": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/run-dev-random-ports.ps1",
    "up": "npm run install:all && npm run dev",
    "start": "npm run up"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
